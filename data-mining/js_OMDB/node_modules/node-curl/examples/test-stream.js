// Generated by ToffeeScript 1.6.2-5
(function() {
  var curl, curl_;

  curl = require('../index');

  curl.setOptions({
    URL: 'www.yahoo.com'
  });

  curl_ = curl.curl_;

  curl_.on_write = function(chunk) {
    console.info(chunk.length);
    return chunk.length;
  };

  curl_.on_error = function(err) {
    return console.info(err);
  };

  curl_.on_end = function() {
    console.info('done.');
    console.info('status:', this.getinfo('RESPONSE_CODE'));
    return console.info('effective url:', this.getinfo('EFFECTIVE_URL'));
  };

  curl_.perform();

}).call(this);
